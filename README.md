# .files

config.fish -> goes into /.config/fish/config.fish

- install starship
- install language servers seperatly. Haven't found out yet how to couple Mason to the new lsp setup in neovim 0.11 that points to the lsp's it finds in the runtime path.
- create ssh key pair for github
- run checkhealth neovim and fix issues

